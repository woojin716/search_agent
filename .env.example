# Copy this file to `.env` and fill in your OpenReview credentials.
# `.env` is gitignored; `.env.example` is the committed template.
OPENREVIEW_USERNAME=you@example.com
OPENREVIEW_PASSWORD=your_password_here
